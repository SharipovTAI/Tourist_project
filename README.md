🏞️ AI-чат-бот по заповедникам России

(на примере Таймырского заповедника)

📌 Описание проекта

Данный проект представляет собой локальный информационный чат-бот, который отвечает на вопросы о заповедниках России строго на основе предоставленного текстового источника (Word-файла).

Проект реализован без использования облачных API и работает полностью офлайн, что делает его подходящим для учебных, исследовательских и демонстрационных целей.

В качестве языковой модели используется Meta LLaMA 3.1, запущенная через llama-cpp-python.

🎯 Цели проекта

Создать чат-бота, который:

отвечает на вопросы о заповедниках;

не придумывает информацию;

использует только локальные данные;

Реализовать подход RAG (Retrieval-Augmented Generation):

поиск релевантной информации;

генерация ответа на её основе;

Интегрировать решение в веб-приложение на Django.

🧱 Архитектура проекта
Пользователь
   ↓
Django API (views.py)
   ↓
Поиск релевантных блоков (SentenceTransformer)
   ↓
Формирование prompt
   ↓
LLaMA (llama-cpp-python)
   ↓
Ответ пользователю

🛠️ Используемый стек технологий
Backend

Python 3.10.9

Django

llama-cpp-python

Модели ИИ

Meta-Llama-3.1-8B-Instruct (GGUF)

SentenceTransformer (all-MiniLM-L6-v2)

Работа с текстом

python-docx

numpy

📂 Структура проекта
tourist_project/
│
├── homepage/
│   ├── views.py          # Основная логика чат-бота
│   ├── urls.py
│   ├── templates/
│   ├── zapovedniki.docx  # Источник знаний
│
├── Llama_project/
│   └── models/
│       └── Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf
│
├── venv/
├── manage.py
└── README.md

📄 Источник данных

Основным источником знаний является файл:

zapovedniki.docx


Файл содержит:

описание Таймырского заповедника;

информацию о других заповедниках России;

геополитические факторы;

исторический и научный контекст.

⚠️ Важно:
Модель не имеет прямого доступа к файлу.
Текст извлекается программно и передаётся в модель через prompt.

🔍 Как работает поиск информации

Word-файл разбивается на текстовые блоки

Каждый блок преобразуется в вектор (эмбеддинг)

Вопрос пользователя также преобразуется в вектор

Выбираются наиболее похожие блоки

Только они передаются в модель

Это позволяет:

уменьшить количество ошибок;

избежать «галлюцинаций» модели;

строго контролировать источник информации.

🚀 Запуск проекта
1. Установка зависимостей
pip install -r requirements.txt


Минимальный набор:

pip install django llama-cpp-python sentence-transformers python-docx numpy

2. Проверка версии Python
python --version


Рекомендуемая версия:

Python 3.10.x

3. Запуск сервера
python manage.py runserver

⚠️ Известные ограничения

Модель может:

путать понятия при плохом контексте;

долго отвечать при слишком большом prompt;

Word-файл должен быть:

структурирован;

логически разделён;

Проект не предназначен для продакшена без доработок.

🔮 Возможные улучшения

Перевод данных из .docx в .json

Кэширование эмбеддингов

Административная панель для редактирования базы знаний

Поддержка нескольких тематических источников

Улучшенная фильтрация по темам (геополитика / природа)

📚 Назначение проекта

Проект создан в учебных и исследовательских целях и может быть использован как:

курсовая работа;

дипломный проект;

демонстрация работы локальных LLM;

пример реализации RAG-подхода без облачных сервисов.

👤 Автор

Проект разработан в рамках индивидуальной разработки.
Все модели и библиотеки используются в соответствии с их лицензиями.
